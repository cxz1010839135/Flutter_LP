#Requires -Version 5.1
<#
.SYNOPSIS
  iOS 打包（占位，后续实现 IPA / Archive）。
#>
$ErrorActionPreference = "Stop"
Write-Host "iOS packaging is not implemented yet." -ForegroundColor Yellow
Write-Host "Planned: flutter build ipa on macOS. See LPROBOT_DEV_RULES.md section 8.1."
exit 1
