<Project Sdk="WixToolset.Sdk/6.0.2">
  <PropertyGroup>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <InstallerPlatform>x64</InstallerPlatform>
    <Platform>x64</Platform>
    <OutputType>Package</OutputType>
    <OutputName>LPRobot</OutputName>
    <EmbedCab>true</EmbedCab>
    <Cultures>zh-CN</Cultures>
    <NuGetAudit>false</NuGetAudit>
    <ReleaseDir Condition="'$(ReleaseDir)' == ''">$(MSBuildThisFileDirectory)..\build\windows\x64\runner\Release</ReleaseDir>
    <ProductVersion Condition="'$(ProductVersion)' == ''">1.4.7.0</ProductVersion>
    <RepoRoot>$(MSBuildThisFileDirectory)..\</RepoRoot>
    <DefineConstants>ProductVersion=$(ProductVersion);RepoRoot=$(RepoRoot)</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="WixToolset.Heat" Version="6.0.2" />
    <PackageReference Include="WixToolset.UI.wixext" Version="6.0.2" />
  </ItemGroup>

  <ItemGroup>
    <HarvestDirectory Include="$(ReleaseDir)">
      <ComponentGroupName>AppPayload</ComponentGroupName>
      <DirectoryRefId>INSTALLFOLDER</DirectoryRefId>
      <Platform>x64</Platform>
      <SuppressRootDirectory>true</SuppressRootDirectory>
      <SuppressCom>true</SuppressCom>
      <SuppressRegistry>true</SuppressRegistry>
    </HarvestDirectory>
    <BindPath Include="$(ReleaseDir)" />
    <BindPath Include="$(RepoRoot)" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="wix\Package.wxs" />
  </ItemGroup>

  <!--
    Cultures=zh-CN 会把 MSI 输出到 zh-CN 子目录。
    同步到 Release 根目录，确保外部打包脚本不会读取历史遗留 MSI。
  -->
  <Target Name="MirrorLocalizedMsiToReleaseRoot" AfterTargets="Build">
    <PropertyGroup>
      <LocalizedMsiPath>$(MSBuildProjectDirectory)\bin\$(Platform)\$(Configuration)\zh-CN\LPRobot.msi</LocalizedMsiPath>
      <RootMsiPath>$(MSBuildProjectDirectory)\bin\$(Platform)\$(Configuration)\LPRobot.msi</RootMsiPath>
    </PropertyGroup>
    <Error
      Condition="!Exists('$(LocalizedMsiPath)')"
      Text="Localized MSI not found: $(LocalizedMsiPath)" />
    <Copy
      SourceFiles="$(LocalizedMsiPath)"
      DestinationFiles="$(RootMsiPath)"
      SkipUnchangedFiles="false" />
  </Target>

</Project>
