# 2026-06-02 变更详情

## 背景

启动 LPRobot Android 1.4.7 → Flutter（`flutter_application_1`）移植；确认 **Windows 优先**、**Android 预留**。

## 架构与路径

- 确立安装目录：`config/`（配置）+ `files/`（保存文件）+ `dll/visualprogram/`
- 实现 `lib/core/robot_paths.dart`、`robot_path_layout.dart`
- Windows 路径实现；Android 外置存储桩 `RobotPathsAndroid`
- `main()` 调用 `RobotPaths.ensureLayout()`，迁移 `config/xml` → `files/xml`

## 网络与状态

- `lib/network/http_manager.dart`：`connect` + `connectAndApply`
- `lib/core/robot_state.dart`：连接状态
- `lib/core/local_app_settings.dart`：`config/app_settings.json` 存 IP

## UI

- `lib/app/lp_robot_colors.dart`、`lp_robot_theme.dart`
- `features/connect/connect_page.dart`：居中布局 + 橙色主题
- `features/home/main_home_page.dart`：模块入口占位
- `LpBrandLogo` + `config/imgs/` 公司 Logo

## Blockly

- 沿用 `dll/visualprogram` + shelf 本地服务
- `lp_blockly_bridge.dart`：`saveServerProject`、`saveXML`→projects、`saveFunXML`→funlib
- API：`/api/files/server/*`、`/api/files/xml/*` 指向新路径

## 文档

- `LPROBOT_DEV_RULES.md` v1.0.0
- `docs/migration/LPROBOT_MIGRATION_PLAN.md` v1.0.0
- `docs/migration/changelog/` 本日志

## 未包含（下一迭代）

- ~~Program 退出上传~~ → **已实现**（待真机联调）
- 连接页接入 `connectSyncAndApply` / 从机器人下载 `main.*`
- Main 轮询、Control / Monitor / Tool 页面
- HttpManager 状态/运动域在 UI 层调用

## 下午（网络 + Blockly 收尾）

### 网络协议（`lib/network/`）

- `RobotHttpClient`、`RobotApiResponse`、`RobotCommands`
- 分模块 mixin：connect / files / motion / state / plc / points / driver / tech
- `syncServerProgramFromRobot`、`uploadServerProgram`、`connectSyncAndApply`
- 联调入口：`HttpManager.invoke` / `invokeRaw`

### Blockly 退出与进度 UI（`lib/blockly/`）

- 退出：离线 → 保存 `config/server` 并返回；在线 → 上传后返回，失败可重传
- 加载进度：shelf GET 计数 + 静默 1.2s 判定完成（避免 JS loadComplete 过早）
- Windows：`LpBlocklyWebViewHost` 隐藏原生 WebView，遮罩不被覆盖
- 加载文案：仅显示「正在加载 Blockly…」等，不显示文件名

## 涉及主要文件

- 网络：`lib/network/**`（12 个 Dart）
- Blockly：`lib/blockly/**`（9 个 Dart）
- 桥接：`dll/.../flutter_bound.js`
